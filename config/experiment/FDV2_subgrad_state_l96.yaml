# @package _global_
# FDV2 variant using update_input=subgrad+state: a cheap two-residual proxy
# gradient (obs residual + prior-autoencoder residual, ported from
# ronan_devs' GradSolver_withStep -- see models/fourdvarnet.py's
# _build_update_input docstring) instead of a real torch.autograd.grad of
# var_cost. Never calls autograd.grad/create_graph -- architecturally immune
# to the double-backward instability that motivated FDV2_grad_state_l96's
# prior_weight/aux-loss/LR-discount recipe. Not in _AUTOGRAD_MODES, so
# trainable_prior_weight has no effect here (prior_weight stays fixed at 1.0,
# used only by the aux_var_cost_weight regularizer term in compute_loss,
# which is generic across all _PRIOR_MODES). Otherwise an exact copy of
# FDV2_grad_state_l96.yaml (same training loss, LR, scheduler, LR-discount
# param groups, architecture scale) for a like-for-like comparison against
# the grad+state recipe (job 52305).
defaults:
  - /lorenz96_default
  - _self_

model:
  model_type: fourdvarnet
  state_dim: 24
  param_dim: 0
  fdv:
    hidden_channels: [64, 128, 256]
    time_emb_dim: 64
    N_outer: 10
    dropout: 0.1
    update_input: subgrad+state
    R_var: 0.5
    prior_weight: 1.0
    clip_range: 50.0
    trainable_prior_weight: true
    aux_var_cost_weight: 0.1

training:
  stage1:
    epochs: 400
    lr: 0.001
    gradient_clip_val: 10.0
    use_cosine_scheduler: true
    obs_weight_lr_scale: 0.5
    prior_unet_lr_scale: 0.5
  stage2:
    epochs: 0

data:
  data_setup: s0_s1
  obs_j: 2
  param_dim: 0
  num_train_windows: 1000
  num_val_windows: 100
  num_test_windows: 200
  test_param_noise: 0.2
  normalize: false
