# @package _global_
defaults:
  - /lorenz63_default
  - _self_

note: "Gaussian estimator only -- no non-Gaussian residual"
training:
  stage2:
    epochs: 0
  batch_size: 32
model:
  hidden_channels: [64, 128, 256]
